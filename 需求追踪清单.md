# 需求追踪清单

## 2026-09-24：个人站信息
小猫的规矩：所有上架中文个人站（kb.kkeist.com）的作品，都必须包含个人站信息（哐哐工作室标注），放在导出 / 设置 / 暂停这类页面里选一处。

- [x] 1. 深度角色问卷在个人站的作品列表里，但页面里没有个人站信息（检查工具 `站点总表/tools/check-credit.py` 报出的）。已补：主页最下面、分类卡片下面放一处标注（这个作品只有主页、完整问卷页、抽卡页三个页面，没有设置 / 导出 / 暂停页，主页是用户最先看到、想找时也最容易找到的一处，标注是页面里的普通一块，不固定在屏幕底部，不打断问卷）
- [x] 2. 样式：头像 48×48 在左，三行文案在右，第二行「全网同名，更多好玩的请前往→」和链接「哐哐的个人站。」连读成一句；背景、边框、圆角、阴影用页面现有变量，和分类卡片同一套；链接可点范围放大到手指好点的大小
- [x] 3. 头像 `kk-avatar.png` 加进发布白名单（`tools/build.mjs`），构建自检通过；`tools/build.test.mjs` 新增一条测试守着：标注只有一处、三行文案齐全、链接指向个人站且新窗口打开、头像进发布目录
- [x] 自测（无头 Chrome + 调试协议，真实点击）：PC 1280×800、手机 390×780、320×640 三种尺寸：标注可见、头像加载 48×48 在文字左边、链接地址和新窗口打开正确（点击后多开一个 kb.kkeist.com 页面、当前页没有跳走）、内容都在标注框内、没有横向溢出、在分类卡片下面不重合；加标注前后主页上方所有区块（标题、说明、随机按钮、分类区）的位置和大小逐个相同
- [ ] 已知限制：真机（安卓、iOS）没有测过，只用 Chrome 的手机尺寸模拟过
