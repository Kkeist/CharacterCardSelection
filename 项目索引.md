# 项目索引

## 是什么，给谁用
深度角色问卷：从 11 个分类的问题里抽卡、复制问题，或查看并复制完整问卷。给想更深入了解自己、或深度塑造角色的人用。纯静态网页，只有中文，没有后端。

## 启动与构建
- 没有开发服务器：用任意静态服务器打开仓库根目录即可（`index.html` 直接引用同目录的脚本和样式）。
- 构建：`node tools/build.mjs`，把发布需要的文件按白名单拷进 `dist/` 并自检，不过就报错退出。
- 测试：`node --test tools/build.test.mjs`。

## 目录结构
| 文件 | 放什么 |
|---|---|
| `index.html` | 三个页面：主页、完整问卷页、抽卡页，都在这一个文件里，靠 `.page.active` 切换 |
| `app.js` | 全部交互：分类卡片生成、抽卡、复制、页面切换 |
| `questions.js` | 题库（问题、提示、分类）；`questions.backup.js` 是备份，不发布 |
| `styles.css` | 全部样式，颜色间距走文件顶部的 `:root` 变量 |
| `kk-avatar.png` | 哐哐工作室标注的头像 |
| `tools/build.mjs` | 发布构建：白名单 `PUBLISH_FILES` 里的文件才会进 `dist/`；新增要发布的文件必须加进这张表 |
| `tools/build.test.mjs` | 构建与发布相关的自动测试 |
| `tools/_headers` | Cloudflare Pages 的缓存规则，构建时拷成 `dist/_headers` |
| `wrangler.toml` `netlify.toml` | 发布目录都写 `dist` |

## 部署（Cloudflare Pages，Git 直连）
- 仓库 `Kkeist/CharacterCardSelection`，分支 main，线上 `https://char-card.kkeist.com`。
- 构建命令 `node tools/build.mjs`，输出目录 `dist`。只有 `dist/` 里的东西会公开。

## 约定
- **哐哐工作室标注（个人站信息）**：这是上架中文个人站的作品，规矩是必须有，放一处：主页最下面（分类卡片下面）的 `<aside class="kk-credit">`。头像 48×48 在左，三行文案在右，第二行和个人站链接连读；文案逐字取自色鬼那一版；链接 `https://kb.kkeist.com/`，新窗口打开。项目只有中文，所以只放中文那套。不许再加第二处。
- 新增要发布的文件（图片、脚本）：加进 `tools/build.mjs` 的 `PUBLISH_FILES`，否则自检会报「不在发布白名单内」。

## 待办与已知问题
见 `需求追踪清单.md`。
